# Port the server listens on (default: 3000)
PORT=3000

# Node environment: "development" (dev Morgan logging) or "production" (combined logging)
NODE_ENV=development

# CORS allowed origin — use '*' for local dev, restrict to your host in production
CORS_ORIGIN=*
